apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "otel-demo.fullname" . }}-test-connection"
  labels:
    {{- include "otel-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "otel-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
