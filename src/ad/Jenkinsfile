pipeline {
    agent any

    stages {
        stage('Check AWS Identity') {
            steps {
                sh '''
                    echo "Running as AWS user:"
                    aws sts get-caller-identity
                '''
            }
        }
    }
}

