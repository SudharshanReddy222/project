pipeline {
    agent any
    tools{
        gradle 'gradle-8'
        jdk 'openjdk-17'
    }

    stages {
        stage('Build microservice') {
            steps {
                dir('project/src/ad') {
                    sh './gradlew clean build'
                }
            }
        }
    }
}
