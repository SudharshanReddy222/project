pipeline {
    agent any
    tools{
        
        jdk 'openjdk_17'
    }

    stages {
        stage('Build microservice') {
            steps {
                dir('project/src/ad') {
                    sh './gradlew clean build'
                }
            }
        }
    }
}
